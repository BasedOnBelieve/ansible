pipeline {
    agent {label 'build'}

    stages {
        stage('Clone Code') {
            steps {
                git url: 'https://github.com/BasedOnBelieve/food3.git'
            }
        }
    }
}